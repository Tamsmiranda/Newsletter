<h1>Leads creation</h1>

<form action="{{ path('newsletter_leads_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('newsletter_leads') }}">
            Back to the list
        </a>
    </li>
</ul>
