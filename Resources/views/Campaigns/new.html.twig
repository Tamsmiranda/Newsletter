<h1>Campaigns creation</h1>

<form action="{{ path('newsletter_campaigns_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('newsletter_campaigns') }}">
            Back to the list
        </a>
    </li>
</ul>
