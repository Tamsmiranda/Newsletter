<h1>Leads list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
            <th>Lastdispatch</th>
            <th>Enable</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('newsletter_leads_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.email }}</td>
            <td>{% if entity.lastDispatch %}{{ entity.lastDispatch|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ entity.enable }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('newsletter_leads_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('newsletter_leads_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('newsletter_leads_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
