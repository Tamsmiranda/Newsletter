<h1>Tags creation</h1>

<form action="{{ path('newsletter_tags_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('newsletter_tags') }}">
            Back to the list
        </a>
    </li>
</ul>
