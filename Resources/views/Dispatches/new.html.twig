<h1>Dispatches creation</h1>

<form action="{{ path('newsletter_dispatches_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('newsletter_dispatches') }}">
            Back to the list
        </a>
    </li>
</ul>
